/// Shared app metadata that both a presentation screen (About) and an
/// application-layer controller (device registration's `app_version` field)
/// need, without either depending on the other.
const String appVersion = '1.0.0';
