org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Disabled: the Kotlin build-tools-api incremental cache crashes on Windows
# when the project (D:) and the pub cache (C:) are on different drives —
# it tries to relativize paths across drive roots. Non-incremental Kotlin
# compilation avoids the bug; it only costs a slightly slower clean build.
kotlin.incremental=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
